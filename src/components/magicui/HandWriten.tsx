import { HandWrittenTitle } from "@/components/ui/hand-writing-text";

function HandWriten({ title, subTitle }: any) {
  return <HandWrittenTitle title={title} subtitle={subTitle} />;
}

export { HandWriten };
